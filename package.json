{
  "name": "envshield",
  "private": true,
  "version": "0.1.1",
  "description": "Type-safe environment variable validation library",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run --workspace vitest.workspace.ts",
    "test:coverage": "vitest run --workspace vitest.workspace.ts --coverage",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "fast-check": "^3.22.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
